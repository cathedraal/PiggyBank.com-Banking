export interface walletActionContextItem {
    'Add money': {container: string, button: string};
    'Transfer money': {container: string, button: string};
    'Withdraw money': {container: string, button: string}
}